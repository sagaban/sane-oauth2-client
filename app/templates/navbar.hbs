<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#main-navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      {{#link-to 'index' class="navbar-brand"}}Ember Simple Auth{{/link-to}}
    </div>
    {{#if sessionAccount.account}}
      <p class="navbar-text {{!--pull-right--}}">{{sessionAccount.account.email}}</p>
    {{/if}}
    <div class="collapse navbar-collapse" id="main-navbar">
      <ul class="nav navbar-nav navbar-right">
            {{#link-to 'index' tagName="li"}}<a href="">Home</a>{{/link-to}}
            {{#link-to 'users-list' tagName="li"}}<a href="">Users</a>{{/link-to}}
            {{#link-to 'messages' tagName="li"}}<a href="">Mesagges</a>{{/link-to}}
            {{#if session.isAuthenticated}}
              <li>
                <a {{action 'invalidateSession'}}>Logout
                <span class="glyphicon glyphicon-log-out" aria-hidden="true"></span>
                </a>
              </li>

            {{else}}
            {{#link-to 'login' tagName="li"}}<a href="">Login
              <span class="glyphicon glyphicon-log-in" aria-hidden="true"></span>
            </a>{{/link-to}}
            {{/if}}
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
